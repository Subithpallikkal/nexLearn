# NexLearn — Frontend

The **project documentation** (setup, env vars, scripts, deployment) is in the **[root `README.md`](../README.md)** next to this folder.

Quick start from repo root:

```bash
cd frontend
npm install
npm run dev
```

For production:

```bash
npm run build
npm start
```
